import React from 'react'
import GalleryImageView from './galleryImage-view'
import { Props } from './models'

const GalleryImage = (props : Props ) => {
  return (
    <GalleryImageView/>
  )
}

export default GalleryImage